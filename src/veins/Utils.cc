#include <map>
#include <string>
#include <vector>
#include <unordered_map>
#include <set>
#include "XMLProcessor.h"
#include <limits>
#include <sstream>
#include <vector>
#include <string>

//std::vector<std::string> Utils::S2Vec(std::string s){
//    std::istringstream iss(s);
//    std::vector<std::string> vecEdges;
//    std::string token;
//    while(iss>>token){
//        vecEdges.push_back(token);
//    }
//    return vecEdges;
//}
//
//std::string Utils::S2Edge(std::string s){
//    std::istringstream iss(s);
//    std::string token;
//    iss>>token;
//    return token;
//}
